# Quickstart

1. 安装依赖：`pip install -r requirements.txt`。
2. 进入CheckData目录运行：`python main.py`。
3. 左侧依次选择 `sport/event → clips|frames → ID`，按 **L**（或点击「Load Data」）载入。
4. 媒体默认读取 `../Dataset/...`，标注读取 `../output/...`
5. 使用 `README.md` 中列出的快捷键完成审核、编辑与保存。
